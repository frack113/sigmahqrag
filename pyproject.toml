[project]
name = "SigmaHQ-RAG"
version = "0.1.0"
description = "A RAG for the SigmaHQ repos"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.0",
    "gradio>=6.12.0",
]
