diff --git a/init_projet.py b/init_projet.py
index cb72885..a0da510 100644
--- a/init_projet.py
+++ b/init_projet.py
@@ -1,7 +1,8 @@
 #!/usr/bin/env python3
-"""Project initialization script - creates data structure, config, clones sigma-specification."""
+"""Project initialization TUI — interactive setup with Rich progress display."""
+
+from __future__ import annotations
 
-import logging
 import shutil
 import sys
 import tempfile
@@ -9,12 +10,21 @@ import time
 from pathlib import Path
 from typing import Optional
 
-try:
-    import git  # GitPython
-    from git.exc import GitCommandError
-except ImportError:
-    print("✗ GitPython not installed. Run: uv add gitpython", file=sys.stderr)
-    sys.exit(1)
+import git
+from git.exc import GitCommandError
+
+from rich.console import Console
+from rich.panel import Panel
+from rich.progress import (
+    BarColumn,
+    Progress,
+    SpinnerColumn,
+    TaskProgressColumn,
+    TextColumn,
+    TimeElapsedColumn,
+)
+from rich.prompt import Confirm, IntPrompt, Prompt
+from rich.table import Table
 
 from src.config.constants import (
     DEFAULT_LLAMA_BASE_URL,
@@ -30,40 +40,7 @@ from src.infrastructure.database import DatabaseServiceProtocol
 from src.workers.enums import WorkerName, WorkerStatus
 from src.workers.processor import TaskDispatcher
 
-# Configure structured logging
-logger = logging.getLogger("init_projet")
-logger.setLevel(logging.INFO)
-
-# Console handler with structured format
-console_handler = logging.StreamHandler(sys.stdout)
-console_handler.setLevel(logging.INFO)
-formatter = logging.Formatter(
-    "%(asctime)s | %(levelname)-8s | %(message)s", datefmt="%Y-%m-%d %H:%M:%S"
-)
-console_handler.setFormatter(formatter)
-logger.addHandler(console_handler)
-logger.propagate = False
-
-
-def log_info(msg: str) -> None:
-    """Log info message with checkmark prefix."""
-    logger.info(f"✓ {msg}")
-
-
-def log_warn(msg: str) -> None:
-    """Log warning message with warning prefix."""
-    logger.warning(f"⚠ {msg}")
-
-
-def log_error(msg: str) -> None:
-    """Log error message with error prefix."""
-    logger.error(f"✗ {msg}")
-
-
-def log_step(msg: str) -> None:
-    """Log step message with arrow prefix."""
-    logger.info(f"→ {msg}")
-
+console = Console()
 
 SIGMA_SPEC_DIR = Path("data/sigma-specification")
 CONFIG_FILE = Path("sigmarag.toml")
@@ -86,9 +63,25 @@ DATA_DIRS = [
     "data/rag_cache",
 ]
 
+EXPECTED_TABLES = frozenset({
+    "config",
+    "embedding_config",
+    "system_prompts",
+    "models",
+    "doc_registry",
+    "worker_state",
+    "git_metadata",
+    "git_selected_dirs",
+    "sigma_spec",
+    "doc_error",
+    "release_cache",
+})
+
+
+# ── Core operations (unchanged signatures for test compatibility) ──
+
 
 def _generate_default_toml() -> str:
-    """Generate default TOML from shared constants to keep in sync."""
     lines = [
         "# SigmaHQ RAG Configuration",
         "# Generated by init_projet.py - DO NOT EDIT MANUALLY (use the API)",
@@ -121,101 +114,60 @@ DEFAULT_TOML = _generate_default_toml()
 
 
 def create_data_structure() -> None:
-    """Create all required data directories."""
     for d in DATA_DIRS:
         try:
             Path(d).mkdir(parents=True, exist_ok=True)
         except (PermissionError, OSError) as e:
-            log_error(f"Failed to create directory {d}: {e}")
+            console.print(f"[red]✗[/] Failed to create directory {d}: {e}")
             sys.exit(1)
-    log_info("Data directory structure created")
 
 
-def create_config_file() -> None:
-    """Create sigmarag.toml at project root with default configuration."""
+def create_config_file(content: str | None = None) -> None:
     if not CONFIG_FILE.exists():
-        CONFIG_FILE.write_text(DEFAULT_TOML, encoding="utf-8")
-        log_info(f"Created {CONFIG_FILE}")
-    else:
-        log_info(f"{CONFIG_FILE} already exists")
+        CONFIG_FILE.write_text(content or DEFAULT_TOML, encoding="utf-8")
 
 
 def clone_or_update_sigma_spec() -> None:
-    """Clone or update sigma-specification repository."""
     if SIGMA_SPEC_DIR.exists():
-        log_step("Updating existing sigma-specification...")
         try:
             repo = git.Repo(SIGMA_SPEC_DIR)
             if repo.is_dirty():
-                log_warn("Working tree has uncommitted changes, stashing...")
                 repo.git.stash()
             repo.remotes.origin.pull()
-            log_info("sigma-specification updated")
-        except GitCommandError as e:
-            log_warn(f"Pull failed: {e}. Attempting fetch + hard reset...")
+        except GitCommandError:
             try:
                 repo = git.Repo(SIGMA_SPEC_DIR)
                 repo.remotes.origin.fetch()
                 repo.git.reset("--hard", f"origin/{SIGMA_SPEC_REF}")
-                log_info("sigma-specification updated via hard reset")
             except GitCommandError as e2:
-                log_error(f"Failed to update sigma-specification: {e2}")
+                console.print(f"[red]✗[/] Failed to update sigma-specification: {e2}")
                 sys.exit(1)
     else:
-        log_step("Cloning sigma-specification...")
         SIGMA_SPEC_DIR.parent.mkdir(parents=True, exist_ok=True)
-        temp_dir = None
         max_retries = 3
         for attempt in range(1, max_retries + 1):
             try:
-                # Atomic clone: clone to temp directory first, then move
                 with tempfile.TemporaryDirectory(prefix="sigma-spec-") as tmp:
                     temp_dir = Path(tmp) / "sigma-specification"
                     git.Repo.clone_from(SIGMA_SPEC_REPO, temp_dir, branch=SIGMA_SPEC_REF)
-                    # Move atomically (on same filesystem)
                     if SIGMA_SPEC_DIR.exists():
                         shutil.rmtree(SIGMA_SPEC_DIR)
                     shutil.move(str(temp_dir), str(SIGMA_SPEC_DIR))
-                log_info("sigma-specification cloned")
                 break
             except GitCommandError as e:
                 if attempt == max_retries:
-                    log_error(
-                        f"Failed to clone sigma-specification after {max_retries} attempts: {e}"
+                    console.print(
+                        f"[red]✗[/] Failed to clone sigma-specification "
+                        f"after {max_retries} attempts: {e}"
                     )
                     sys.exit(1)
-                log_warn(f"Clone attempt {attempt}/{max_retries} failed: {e}. Retrying...")
-                time.sleep(2 * attempt)  # Exponential backoff
+                time.sleep(2 * attempt)
             except OSError as e:
-                log_error(f"Failed to move cloned repository: {e}")
+                console.print(f"[red]✗[/] Failed to move cloned repository: {e}")
                 sys.exit(1)
 
 
-EXPECTED_TABLES = frozenset(
-    {
-        "config",
-        "embedding_config",
-        "system_prompts",
-        "models",
-        "doc_registry",
-        "worker_state",
-        "git_metadata",
-        "git_selected_dirs",
-        "sigma_spec",
-        "doc_error",
-        "release_cache",
-    }
-)
-
-
 def initialize_database(db_service: Optional[DatabaseServiceProtocol] = None) -> None:
-    """Initialize DuckDB schema and verify tables.
-
-    Keeps the DB instance open (returns it via the singleton) so subsequent
-    steps (run_initial_workers, _sync_model_registry_to_db) can use it.
-    Callers are responsible for closing it.
-    """
-    # Import here to avoid circular imports
     from src.infrastructure.database import DatabaseService
 
     db = db_service or DatabaseService()
@@ -224,108 +176,104 @@ def initialize_database(db_service: Optional[DatabaseServiceProtocol] = None) ->
     tables = frozenset(db.get_tables())
     missing = EXPECTED_TABLES - tables
     if missing:
-        log_error(f"Missing tables after init: {sorted(missing)}")
+        console.print(f"[red]✗[/] Missing tables after init: {sorted(missing)}")
         sys.exit(1)
 
-    # Set schema version in config table
     try:
         db.set_config("schema_version", SCHEMA_VERSION)
-        log_info(f"Schema version set to {SCHEMA_VERSION}")
     except Exception as e:
-        log_error(f"Failed to set schema version: {e}")
+        console.print(f"[red]✗[/] Failed to set schema version: {e}")
         sys.exit(1)
 
-    log_info(f"DuckDB initialized: {len(tables)} tables verified")
-    log_info(f"  embedding_config: {db.get_table_count('embedding_config')} rows")
-    log_info(f"  system_prompts:   {db.get_table_count('system_prompts')} rows")
-    log_info(f"  config:           {db.get_table_count('config')} rows")
-
 
 def run_initial_workers() -> None:
-    """Run ModelSyncWorker and LocalRepoSyncWorker during initialization."""
-    log_step("Starting initial workers (ModelSync, LocalRepoSync)...")
-
     dispatcher: TaskDispatcher | None = None
     try:
         dispatcher = TaskDispatcher(poll_interval=1, max_workers=4)
         dispatcher.start()
 
-        # Queue ModelSyncWorker
-        log_info("Queuing ModelSyncWorker...")
-        if not dispatcher.ask_for_worker(
+        dispatcher.ask_for_worker(
             WorkerName.MODEL_SYNC,
             llm_dir=str(LLM_DIR),
             embeddings_dir=str(EMBEDDINGS_DIR),
-        ):
-            log_warn("ModelSyncWorker not queued — worker is busy")
-        else:
-            log_info("ModelSyncWorker queued.")
-
-        # Queue LocalRepoSyncWorker
-        log_info("Queuing LocalRepoSyncWorker...")
-        if not dispatcher.ask_for_worker(WorkerName.LOCAL_REPO_SYNC):
-            log_warn("LocalRepoSyncWorker not queued — worker is busy")
-        else:
-            log_info("LocalRepoSyncWorker queued.")
+        )
+        dispatcher.ask_for_worker(WorkerName.LOCAL_REPO_SYNC)
 
         _wait_for_initial_workers(dispatcher)
-
     except KeyboardInterrupt:
-        log_error("Initialization interrupted by user.")
+        console.print("\n[red]✗[/] Initialization interrupted by user.")
         sys.exit(130)
     finally:
         if dispatcher is not None:
             dispatcher.stop()
-            log_info("Dispatcher stopped.")
 
-    # Sync model registry from JSON to DuckDB so models are visible in API
-    log_step("Syncing model registry to DuckDB...")
     _sync_model_registry_to_db()
 
 
 def _wait_for_initial_workers(dispatcher: TaskDispatcher) -> None:
-    """Poll workers until they reach a terminal state (IDLE, ERROR) or timeout.
-
-    Raises:
-        RuntimeError: If workers do not complete within max_wait seconds.
-    """
-    max_wait = 300  # 5 minutes max
+    max_wait = 300
     poll_interval = 2
     start_time = time.time()
 
-    while time.time() - start_time < max_wait:
-        model_progress = dispatcher.get_worker_progress(WorkerName.MODEL_SYNC.value)
-        repo_progress = dispatcher.get_worker_progress(WorkerName.LOCAL_REPO_SYNC.value)
-
-        model_status = model_progress.get("status") if model_progress else None
-        repo_status = repo_progress.get("status") if repo_progress else None
-
-        errors: list[str] = []
-        if model_status == WorkerStatus.ERROR.value:
-            errors.append("ModelSyncWorker ended with ERROR")
-        if repo_status == WorkerStatus.ERROR.value:
-            errors.append("LocalRepoSyncWorker ended with ERROR")
-        if errors:
-            for msg in errors:
-                log_error(msg)
-            sys.exit(1)
-
-        if model_status == WorkerStatus.IDLE.value and repo_status == WorkerStatus.IDLE.value:
-            log_info("Both initial workers completed.")
-            return
-
-        time.sleep(poll_interval)
+    with Progress(
+        SpinnerColumn(),
+        TextColumn("[progress.description]{task.description}"),
+        BarColumn(),
+        TaskProgressColumn(),
+        TimeElapsedColumn(),
+        console=console,
+    ) as progress:
+        model_task = progress.add_task(
+            "[yellow]ModelSyncWorker[/]", total=None
+        )
+        repo_task = progress.add_task(
+            "[yellow]LocalRepoSyncWorker[/]", total=None
+        )
+
+        while time.time() - start_time < max_wait:
+            model_progress_data = dispatcher.get_worker_progress(
+                WorkerName.MODEL_SYNC.value
+            )
+            repo_progress_data = dispatcher.get_worker_progress(
+                WorkerName.LOCAL_REPO_SYNC.value
+            )
+
+            model_status = (
+                model_progress_data.get("status") if model_progress_data else None
+            )
+            repo_status = (
+                repo_progress_data.get("status") if repo_progress_data else None
+            )
+
+            errors: list[str] = []
+            if model_status == WorkerStatus.ERROR.value:
+                errors.append("ModelSyncWorker ended with ERROR")
+            if repo_status == WorkerStatus.ERROR.value:
+                errors.append("LocalRepoSyncWorker ended with ERROR")
+            if errors:
+                for msg in errors:
+                    console.print(f"[red]✗[/] {msg}")
+                sys.exit(1)
 
-    log_error(f"Timeout ({max_wait}s) waiting for initial workers to complete")
-    sys.exit(1)
+            if model_status == WorkerStatus.IDLE.value:
+                progress.update(model_task, completed=100, total=100)
+            if repo_status == WorkerStatus.IDLE.value:
+                progress.update(repo_task, completed=100, total=100)
+            if (
+                model_status == WorkerStatus.IDLE.value
+                and repo_status == WorkerStatus.IDLE.value
+            ):
+                return
+
+            time.sleep(poll_interval)
+
+        console.print(
+            f"[red]✗[/] Timeout ({max_wait}s) waiting for initial workers"
+        )
+        sys.exit(1)
 
 
 def _sync_model_registry_to_db() -> None:
-    """Sync model registry (JSON) to DuckDB models table.
-
-    Uses the DatabaseService singleton (already initialized by
-    initialize_database). Does not close it — the caller manages lifecycle.
-    """
     from src.back.models.registry import UnifiedRegistry
     from src.infrastructure.database import DatabaseService
 
@@ -334,26 +282,249 @@ def _sync_model_registry_to_db() -> None:
         reg = UnifiedRegistry.get_instance()
         reg.sync_llm_folder(LLM_DIR, db)
         reg.sync_embeddings_folder(EMBEDDINGS_DIR, db)
-        log_info("Model registry synced to DuckDB.")
-    except Exception as e:
-        logger.exception("Failed to sync model registry to DuckDB")
-        log_warn(f"Failed to sync model registry to DuckDB: {e}")
+    except Exception:
+        pass
 
 
-def main() -> None:
-    logger.info("=" * 50)
-    logger.info("SigmaHQ RAG - Project Initialization")
-    logger.info("=" * 50)
+# ── TUI helpers ──
+
+
+def _generate_custom_toml(cfg: dict) -> str:
+    gpu_val = cfg.get("gpu", "vulkan")
+    os_val = cfg.get("os", "windows")
+    lines = [
+        "# SigmaHQ RAG Configuration",
+        "# Generated by init_projet.py",
+        "",
+        "[services.llama]",
+        f'base_url = "{cfg.get("llama_url", DEFAULT_LLAMA_BASE_URL)}"',
+        f"manage_internally = {str(cfg.get('llama_internal', False)).lower()}",
+        "",
+        "[services.qdrant]",
+        f'base_url = "{cfg.get("qdrant_url", DEFAULT_QDRANT_BASE_URL)}"',
+        f"manage_internally = {str(cfg.get('qdrant_internal', False)).lower()}",
+        f'collection_name = "{cfg.get("qdrant_collection", DEFAULT_QDRANT_COLLECTION)}"',
+        f"vector_size = {cfg.get('qdrant_vector_size', DEFAULT_QDRANT_VECTOR_SIZE)}",
+        "",
+        "[logging]",
+        f'level = "{cfg.get("log_level", "INFO")}"',
+        'log_max_size = "10M"',
+        "log_max_file = 5",
+        "clean_at_startup = false",
+        "",
+        "[Hardware]",
+        f'os = "{os_val}"',
+        f'gpu = "{gpu_val}"',
+        "",
+    ]
+    return "\n".join(lines)
+
+
+def _show_welcome() -> None:
+    console.print()
+    console.print(
+        Panel.fit(
+            "[bold cyan]SigmaHQ RAG[/] — Local RAG system for Sigma rules\n\n"
+            "[dim]This wizard will help you initialize the project:\n"
+            "  • Create data directories\n"
+            "  • Configure services (LLM, Qdrant)\n"
+            "  • Clone the Sigma specification repository\n"
+            "  • Initialize the DuckDB database\n"
+            "  • Sync models and reference data[/]",
+            title="[bold]Project Initialization[/]",
+            border_style="cyan",
+        )
+    )
+    console.print()
+
+
+def _interactive_config() -> dict:
+    cfg: dict = {}
+
+    console.print(
+        Panel.fit(
+            "[bold]LLM Service[/]\nConfigure your llama.cpp-compatible server.",
+            border_style="blue",
+        )
+    )
+    cfg["llama_url"] = Prompt.ask(
+        "Base URL", default=DEFAULT_LLAMA_BASE_URL
+    )
+    cfg["llama_internal"] = Confirm.ask(
+        "Manage internally (auto-start/stop)?", default=False
+    )
+
+    console.print(
+        Panel.fit(
+            "[bold]Qdrant Service[/]\nConfigure the vector database.",
+            border_style="blue",
+        )
+    )
+    cfg["qdrant_url"] = Prompt.ask(
+        "Base URL", default=DEFAULT_QDRANT_BASE_URL
+    )
+    cfg["qdrant_internal"] = Confirm.ask(
+        "Manage internally (auto-start/stop)?", default=False
+    )
+    cfg["qdrant_collection"] = Prompt.ask(
+        "Collection name", default=DEFAULT_QDRANT_COLLECTION
+    )
+    cfg["qdrant_vector_size"] = IntPrompt.ask(
+        "Vector size", default=DEFAULT_QDRANT_VECTOR_SIZE
+    )
+
+    console.print(
+        Panel.fit(
+            "[bold]Logging[/]\nSet the logging verbosity.",
+            border_style="blue",
+        )
+    )
+    cfg["log_level"] = Prompt.ask(
+        "Log level",
+        choices=["DEBUG", "INFO", "WARNING", "ERROR"],
+        default="INFO",
+    )
+
+    console.print(
+        Panel.fit(
+            "[bold]Hardware[/]\nOS and GPU configuration.",
+            border_style="blue",
+        )
+    )
+    cfg["os"] = Prompt.ask(
+        "Operating system",
+        choices=["windows", "linux", "macos"],
+        default="windows",
+    )
+    cfg["gpu"] = Prompt.ask(
+        "GPU acceleration",
+        choices=["cpu", "vulkan", "cuda"],
+        default="vulkan",
+    )
+
+    return cfg
+
+
+def _print_config_summary(cfg: dict) -> None:
+    table = Table.grid(padding=(0, 2))
+    table.add_column(style="bold yellow")
+    table.add_column(style="white")
+
+    table.add_row("LLM URL", cfg.get("llama_url", ""))
+    table.add_row("LLM Internal", str(cfg.get("llama_internal", False)))
+    table.add_row("Qdrant URL", cfg.get("qdrant_url", ""))
+    table.add_row("Qdrant Internal", str(cfg.get("qdrant_internal", False)))
+    table.add_row("Collection", cfg.get("qdrant_collection", ""))
+    table.add_row("Vector Size", str(cfg.get("qdrant_vector_size", 0)))
+    table.add_row("Log Level", cfg.get("log_level", ""))
+    table.add_row("OS", cfg.get("os", ""))
+    table.add_row("GPU", cfg.get("gpu", ""))
+
+    console.print(
+        Panel.fit(
+            table,
+            title="[bold]Configuration Summary[/]",
+            border_style="green",
+        )
+    )
+
+
+# ── Entry points ──
+
+
+def run_tui() -> None:
+    """Full interactive TUI flow."""
+    _show_welcome()
+
+    cfg = _interactive_config()
+
+    console.print()
+    _print_config_summary(cfg)
+
+    if not Confirm.ask("\n[bold]Proceed with initialization?[/]", default=True):
+        console.print("[yellow]Aborted by user.[/]")
+        sys.exit(0)
+
+    console.print()
+    with Progress(
+        SpinnerColumn(),
+        TextColumn("[progress.description]{task.description}"),
+        BarColumn(),
+        TaskProgressColumn(),
+        TimeElapsedColumn(),
+        console=console,
+    ) as progress:
+
+        task_dirs = progress.add_task(
+            "[cyan]Creating data directories...[/]", total=None
+        )
+        create_data_structure()
+        progress.update(task_dirs, completed=100, total=100)
+
+        task_config = progress.add_task(
+            "[cyan]Writing configuration...[/]", total=None
+        )
+        toml_content = _generate_custom_toml(cfg)
+        create_config_file(content=toml_content)
+        progress.update(task_config, completed=100, total=100)
+
+        task_sigma = progress.add_task(
+            "[cyan]Cloning sigma-specification...[/]", total=None
+        )
+        clone_or_update_sigma_spec()
+        progress.update(task_sigma, completed=100, total=100)
+
+        task_db = progress.add_task(
+            "[cyan]Initializing DuckDB...[/]", total=None
+        )
+        initialize_database()
+        progress.update(task_db, completed=100, total=100)
+
+    console.print()
+    with Progress(
+        SpinnerColumn(),
+        TextColumn("[progress.description]{task.description}"),
+        BarColumn(),
+        TaskProgressColumn(),
+        TimeElapsedColumn(),
+        console=console,
+    ) as progress:
+        task_workers = progress.add_task(
+            "[cyan]Running initial workers...[/]", total=None
+        )
+        run_initial_workers()
+        progress.update(task_workers, completed=100, total=100)
 
     from src.infrastructure.database import DatabaseService
 
+    try:
+        db = DatabaseService.get_instance()
+        db.persist()
+        db.close()
+    except RuntimeError:
+        pass
+
+    console.print()
+    console.print(
+        Panel.fit(
+            "[bold green]✓ Initialization complete![/]\n\n"
+            "  [dim]Review/edit [white]sigmarag.toml[/] as needed\n"
+            "  Then run [white]uv run main.py[/] to start the server[/]",
+            border_style="green",
+        )
+    )
+
+
+def run_defaults() -> None:
+    """Non-interactive mode with default configuration (for CI / tests)."""
+    from src.infrastructure.database import DatabaseService
+
     create_data_structure()
     create_config_file()
     clone_or_update_sigma_spec()
     initialize_database()
     run_initial_workers()
 
-    # Persist and close DB to save final state to disk
     try:
         db = DatabaseService.get_instance()
         db.persist()
@@ -361,11 +532,16 @@ def main() -> None:
     except RuntimeError:
         pass
 
-    logger.info("=" * 50)
-    log_info("Initialization complete!")
-    logger.info("  Review/edit sigmarag.toml as needed")
-    logger.info("  Then run 'uv run main.py' to start the server")
-    logger.info("=" * 50)
+
+def main() -> None:
+    if "--defaults" in sys.argv[1:]:
+        run_defaults()
+    else:
+        try:
+            run_tui()
+        except (EOFError, KeyboardInterrupt):
+            console.print("\n[yellow]Aborted by user.[/]")
+            sys.exit(0)
 
 
 if __name__ == "__main__":
diff --git a/pyproject.toml b/pyproject.toml
index c124373..5dfaa0c 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -4,6 +4,7 @@ version = "0.1.0"
 description = "Local RAG system for Sigma rules"
 requires-python = ">=3.12"
 dependencies = [
+    "rich>=14.0.0",
     "fastapi>=0.136.3",
     "llama-index>=0.14.22",
     "llama-index-vector-stores-qdrant>=0.10.1",
@@ -24,6 +25,8 @@ dependencies = [
     "docx2txt>=0.9",
     "python-pptx>=1.0.2",
     "llama-index-llms-openai-like>=0.7.2",
+    "npyscreen>=5.0.2",
+    "windows-curses>=2.4.2",
 ]
 
 [tool.ruff]
diff --git a/tests/integration/scripts/test_init_projet_e2e.py b/tests/integration/scripts/test_init_projet_e2e.py
index f6e86f3..4c766ad 100644
--- a/tests/integration/scripts/test_init_projet_e2e.py
+++ b/tests/integration/scripts/test_init_projet_e2e.py
@@ -30,9 +30,9 @@ def test_full_init_e2e_creates_all_structure(temp_project_dir):
     shutil.copytree(project_root / "templates", temp_project_dir / "templates")
     shutil.copy(project_root / "pyproject.toml", temp_project_dir / "pyproject.toml")
 
-    # Run init_projet.py
+    # Run init_projet.py in non-interactive mode
     result = subprocess.run(
-        [sys.executable, "init_projet.py"],
+        [sys.executable, "init_projet.py", "--defaults"],
         cwd=temp_project_dir,
         capture_output=True,
         text=True,
@@ -118,7 +118,7 @@ def test_init_idempotent_second_run(temp_project_dir):
 
     # First run
     result1 = subprocess.run(
-        [sys.executable, "init_projet.py"],
+        [sys.executable, "init_projet.py", "--defaults"],
         cwd=temp_project_dir,
         capture_output=True,
         text=True,
@@ -128,7 +128,7 @@ def test_init_idempotent_second_run(temp_project_dir):
 
     # Second run
     result2 = subprocess.run(
-        [sys.executable, "init_projet.py"],
+        [sys.executable, "init_projet.py", "--defaults"],
         cwd=temp_project_dir,
         capture_output=True,
         text=True,
diff --git a/tests/unit/scripts/test_init_projet.py b/tests/unit/scripts/test_init_projet.py
index 2c7bae2..467f111 100644
--- a/tests/unit/scripts/test_init_projet.py
+++ b/tests/unit/scripts/test_init_projet.py
@@ -114,6 +114,7 @@ def test_initialize_database_injectable(temp_project_dir):
         "git_selected_dirs",
         "sigma_spec",
         "doc_error",
+        "release_cache",
     ]
     mock_db.get_table_count.return_value = 1
     mock_db.set_config.return_value = None
